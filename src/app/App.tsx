import { CardDetails, CardList } from 'features';

const App = () => {
  return (
    <main className="min-h-screen bg-night-sky py-5">
      <CardDetails />
      <CardList />
    </main>
  );
};

export default App;
