import { CardDetails, CardList } from 'features';

const App = () => {
  return (
    <main className="min-h-screen bg-night-sky">
      <CardDetails />
      <CardList />
    </main>
  );
};

export default App;
